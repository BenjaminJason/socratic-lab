# 苏格拉底式学习应用 — 设计笔记（动手前先定）

## 一句话
本地运行的苏格拉底式 AI 导师，中文为主/可切英文，三主题（狭义相对论 / 期权定价 / 计量经济学）从零到深。

## 核心模块
- 知识图谱（内容为核心，需我全部设计）
- AI 导师引擎（Anthropic claude-sonnet-4-6，只提问给提示、不给答案）
- 学习机制（掌握度 0-100、费曼检验、间隔复习、力导向图可视化）
- 数据与界面（持久化、深色、手机优先）

## ⚠️ 动手前必须确认的关键点（见对话）
1. **持久化用什么** —— 需求写"用 window.storage 不用 localStorage"，但 window.storage 非标准 API；本地运行 localStorage 完全可用。需澄清。
2. **React 用哪种形态** —— 关系到内存/是否要构建工具。
3. **内容范围/分期** —— 三主题全量内容极大，建议先做全框架 + 1 主题打样。
4. AI Key：BYO（用户自填 Anthropic Key，本地明文存储，仅本人用）。

## 内存分析结论（详见对话）
- 最大内存/加载开销来源：浏览器内 Babel 转译 JSX（若用 React CDN 方案）。
- 第二来源：力导向图若持续跑物理模拟。
- 结论：只要①避开浏览器内 Babel（用 Preact+htm 或预构建）②图收敛后停止模拟，整体不重。
